# FreshRoute Mobile_Dev

This is a **team skeleton** of the FreshRoute Expo app.

- Navigation route wiring is included under `src/navigation/`.
- Screen/component files are **minimal stubs** (no feature logic).

## Run

```bash
npm install
npm run start
```
